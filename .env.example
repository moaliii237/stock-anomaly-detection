# Database connection for the data warehouse loader and the final notebook.
# Copy this file to .env and fill in your own values. Do not commit .env.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=group_13_warehouse
DB_USER=your_username
DB_PASSWORD=your_password
